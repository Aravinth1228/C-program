#include<stdio.h>
int main(){
    int l,b;
    printf("Enter the length & breath:");
    scanf("%d%d",&l,&b);
    int area = l*b;
     int perimetre = 2*(l+b);
    printf("\n%d",rectanglur);
    printf("\n%d",circle);
    return 0;
}